{% set elems = {
  anchor: 'a',
  button: 'button'
} %}

{% set class = ['button']|merge(classes|default([])) %}

<{{ elems[type|default('anchor')] }} href="{{ href }}" class="{{ class|join(' ') }}">
  {% block content %}
    {{ content }}
  {% endblock %}
</{{ elems[type|default('anchor')] }}>
