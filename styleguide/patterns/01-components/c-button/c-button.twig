{% set elems = {
  anchor: 'a',
  button: 'button'
} %}

{% set class = ['c-button']|merge(classes|default([])) %}

<{{ elems[type|default('anchor')] }} href="{{ href }}" class="{{ class|join(' ') }}">
  {% block content %}
    {{ content }}
  {% endblock %}
</{{ elems[type|default('anchor')] }}>
