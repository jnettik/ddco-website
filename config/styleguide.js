/**
 * @file
 * Config file for the site styleguide.
 */

const config = {};

config.sitename = "Dearly Design co.";
config.theme = {
  skin: "navy"
};
config.componentsTitle = "Patterns";
config.preview = "@preview";

module.exports = config;
