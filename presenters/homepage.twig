{% embed './default.twig' %}
  {% block content %}
    <section class="masthead">
      {%
        include '../styleguide/patterns/01-components/page-title/page-title.twig' with {
          'text': 'Let\'s have',
          'emphasized': 'fun together'
        }
      %}
    </section>

    {{ parent() }}
  {% endblock %}
{% endembed %}
